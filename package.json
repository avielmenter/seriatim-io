{
  "name": "seriatim-editing",
  "version": "0.1.0",
  "description": "The document editing server for Seriatim.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start:dev": "ts-node-dev --respawn --transpile-only ./src/index.ts",
    "start": "node ./dist/index.js",
    "postinstall": "npm run build"
  },
  "author": "Aviel Menter",
  "license": "MIT",
  "devDependencies": {
    "@types/node-fetch": "^2.5.7",
    "@types/redis": "^2.8.25",
    "ts-node-dev": "^1.0.0-pre.44",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "@types/socket.io": "^2.1.8",
    "dotenv": "^8.2.0",
    "immutable": "^4.0.0-rc.12",
    "redis": "^3.0.2",
    "socket.io": "^2.3.0"
  }
}
